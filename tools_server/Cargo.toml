[package]
name = "coding-agent-toolchain"
version = "0.1.0"
edition = "2021"
description = "Rust MCP-style local coding toolchain server"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
walkdir = "2"
glob = "0.3"
